image: gitpod/workspace-full:latest

ports:
  - port: 11434   # Ollama
    onOpen: ignore
    visibility: private

tasks:
  - name: setup
    init: bash .gp/setup.sh
    command: bash -lc 'source ~/.bashrc && echo "AIChat ready. Try: ash \"find biggest files\""'

vscode:
  extensions:
    - ms-python.python
    - ms-python.vscode-pylance


